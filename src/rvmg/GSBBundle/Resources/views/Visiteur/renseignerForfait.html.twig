{% extends "rvmgGSBBundle:Default:baseVisiteur.html.twig" %}

{% block body %}
    <div class="container">
        
        {{ form_start(form) }}
            <div class="ChampForm">{{ form_label(form.quantite) }}</div>
           {{ form_widget(form.quantite) }}
            <div class="ChampForm">{{ form_label(form.idfraisforfait) }}</div>
            {{ form_widget(form.idfraisforfait) }}

        <div class="BoutonForm"><input type="submit" value="Valider" class="btn btn-primary valider" />
            <input type="reset" value="Annuler" class="btn btn-danger valider" /></div>
        
       
        {{ form_end(form) }}
    </div>
{% endblock %}